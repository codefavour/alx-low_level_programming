#include <stdio.h>
/**
 * main - Entry point
 * DESCRIPTION: 'C program for printf'
 * Return: Always return 0 (success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
